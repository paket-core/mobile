<?xml version="1.0" encoding="UTF-8"?>
<local:BasePage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="PaketGlobal.PackagesPage"
             xmlns:local="clr-namespace:PaketGlobal">
    <local:BasePage.Content>
        <AbsoluteLayout>
            
            <ListView x:Name="packagesList" RowHeight="75" AbsoluteLayout.LayoutFlags="All" AbsoluteLayout.LayoutBounds="0, 0, 1.0, 1.0"
                    ItemsSource="{Binding PackagesList}" ItemSelected="PackageItemSelected" IsPullToRefreshEnabled="false">
                  <ListView.ItemTemplate>
                     <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Vertical" Padding="15,8,15,5" Spacing="0">
                                <Label Text="{Binding DeadlineString, StringFormat='Deadline: {0}' Mode=OneWay}" FontSize="17" TextColor="#000000"/>
                                <Label Text="{Binding Status, Mode=OneWay}" FontSize="13" TextColor="#808080"/>
                                <Label Text="{Binding SendTimeString, StringFormat='Sent: {0}' Mode=OneWay}" FontSize="13" TextColor="#808080"/>
                            </StackLayout>
                         </ViewCell>                    
                    </DataTemplate>
                 </ListView.ItemTemplate>
            </ListView>

            <AbsoluteLayout x:Name="layoutActivity" BackgroundColor="#FFFFFFFF" AbsoluteLayout.LayoutFlags="All" AbsoluteLayout.LayoutBounds="0, 0, 1.0, 1.0">
                <ActivityIndicator x:Name="activityIndicator"
                    AbsoluteLayout.LayoutFlags="PositionProportional"
                    AbsoluteLayout.LayoutBounds="0.5, 0.5, AutoSize, AutoSize"
                    Color="#FF4081"
                    IsRunning="false"/>
            </AbsoluteLayout>
            
            <AbsoluteLayout x:Name="layoutPlacholderLabel" BackgroundColor="#FFFFFFFF" AbsoluteLayout.LayoutFlags="All" AbsoluteLayout.LayoutBounds="0, 0, 1.0, 1.0">
                <Label x:Name="placholderLabel" TextColor="#D3D3D3" FontSize = "20" Text="No Packages Found"
                    IsVisible = "false"
                    AbsoluteLayout.LayoutFlags="PositionProportional"
                    AbsoluteLayout.LayoutBounds="0.5, 0.5, AutoSize, AutoSize"/>
            </AbsoluteLayout>
            
        </AbsoluteLayout>
    </local:BasePage.Content>
</local:BasePage>
